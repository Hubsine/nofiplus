{% if routeName is defined and routeParameters is defined %}
    <div class="d-inline-block" type="button">
        <a class="btn button" href="{{ path( routeName, routeParameters ) }}" title="Mettre à jour">
            <i class="fas fa-edit"></i>
        </a>
    </div>
{% endif %}
